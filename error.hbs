{{!< default}}

<section class="photo-error">
    <span class="photo-error-code">{{statusCode}}</span>
    <h1>{{message}}</h1>
    <p>The page you're looking for doesn't exist or has been moved.</p>
    <a href="{{@site.url}}">Back to gallery</a>
</section>
