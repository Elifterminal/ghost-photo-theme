{{!< default}}

<header class="photo-tag-header photo-reveal">
    <h1>{{name}}</h1>
    {{#if description}}
        <p>{{description}}</p>
    {{/if}}
    <span class="photo-tag-count">{{plural ../pagination.total empty="No posts" singular="% post" plural="% posts"}}</span>
</header>

<section class="photo-gallery">
    <div class="photo-masonry photo-stagger">
        {{#foreach posts}}
        <article class="photo-card photo-reveal">
            <a href="{{url}}" aria-label="{{title}}">
                <div class="photo-card-image">
                    {{#if feature_image}}
                        <img src="{{img_url feature_image size="l"}}"
                             srcset="{{img_url feature_image size="s"}} 300w,
                                     {{img_url feature_image size="m"}} 600w,
                                     {{img_url feature_image size="l"}} 1200w"
                             sizes="(max-width: 600px) 100vw, (max-width: 1024px) 50vw, 33vw"
                             alt="{{title}}"
                             loading="lazy">
                    {{/if}}
                </div>
                <div class="photo-card-overlay">
                    <h2 class="photo-card-title">{{title}}</h2>
                    <span class="photo-card-meta">{{date format="MMM D, YYYY"}}</span>
                </div>
            </a>
        </article>
        {{/foreach}}
    </div>
</section>

{{#if pagination.pages}}
<nav class="photo-pagination" aria-label="Pagination">
    {{#if pagination.prev}}
        <a href="{{pagination.prev}}" aria-label="Previous page">&larr; Newer</a>
    {{/if}}
    <span>Page {{pagination.page}} of {{pagination.pages}}</span>
    {{#if pagination.next}}
        <a href="{{pagination.next}}" aria-label="Next page">Older &rarr;</a>
    {{/if}}
</nav>
{{/if}}
